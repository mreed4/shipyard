import { massProduceShips } from "../functions/massProduceShips.js";

// Test function to run the file in the terminal
const testShips = massProduceShips(20);
console.log("Generated Ship IDs:", testShips);
